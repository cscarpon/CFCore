% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_pointcloud.R
\name{align_las_icp_voxelized}
\alias{align_las_icp_voxelized}
\title{Align two LAS point clouds using ICP after voxelization}
\usage{
align_las_icp_voxelized(
  source_las,
  target_las,
  output_las_path,
  voxel_size = 0.2,
  max_iter = 50,
  threads = 12
)
}
\arguments{
\item{source_las}{\code{lidR::LAS} reference point cloud (fixed).}

\item{target_las}{\code{lidR::LAS} point cloud to align to the source (moving).}

\item{output_las_path}{Output path for \code{lidR::writeLAS()}.}

\item{voxel_size}{Numeric voxel size passed to \code{lidR::voxelize_points()}.}

\item{max_iter}{Integer ICP iterations passed to \code{Morpho::icpmat()}.}

\item{threads}{Integer thread count passed to \code{Morpho::icpmat()}.}
}
\value{
A voxelized \code{lidR::LAS} object representing the aligned target.
}
\description{
Voxelizes both point clouds, runs \code{Morpho::icpmat()} to align target to source,
and writes an aligned LAS to disk. Metadata is transferred from the voxelized
target points via \code{point_source_id}.
}
\details{
Important: This returns a voxel-level LAS, not a full-resolution aligned LAS.
If you need full resolution alignment, you should apply the resulting
transformation to the original target point matrix.
}
\keyword{internal}
